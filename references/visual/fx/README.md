# fx reference catalog

0 indexed image groups in `docs/references/visual/reference_manifest.json` (filter category `fx`).

Binaries remain at indexed legacy paths to avoid duplication. Archive-origin entries need the read-only `.work/legacy-source/` copy. Rights remain unknown; these are reference-only. No samples admitted yet.
