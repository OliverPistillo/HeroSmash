export class ParticleSystem{constructor(){this.p=[]}burst(x,y,col='#fff',n=12){for(let i=0;i<n;i++)this.p.push({x,y,vx:(Math.random()-.5)*130,vy:(Math.random()-.8)*130,life:.8,col,s:2+Math.random()*4})}update(dt){for(const p of this.p){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=160*dt}this.p=this.p.filter(p=>p.life>0)}draw(r){const c=r.ctx;for(const p of this.p){c.globalAlpha=Math.max(0,p.life/.8);c.fillStyle=p.col;c.beginPath();c.arc(p.x,p.y,p.s,0,Math.PI*2);c.fill();c.globalAlpha=1}}}
