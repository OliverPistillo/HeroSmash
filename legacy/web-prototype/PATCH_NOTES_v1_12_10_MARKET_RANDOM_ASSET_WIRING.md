# HeroSmash v1.12.10 — Market / Random Asset Wiring

## Modifiche
- `MARKET` usa `assets/ui/market/btn_market.png`.
- `RANDOM` usa l’asset fornito `assets/ui/market/btn_random.png`.
- `LOCK` e `UNLOCK` sono asset separati e si alternano in base allo stato dello shop.
- Aggiornati `data/ui_assets.json` e `data/runtime.json`.

## File obsoleto
- `assets/ui/market/btn.market.png` può essere rimosso: è stato normalizzato in `btn_market.png`.
