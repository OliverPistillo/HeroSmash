diff --git a/src/blender_mcp/consent_prompt.py b/src/blender_mcp/consent_prompt.py
index e507329ca582555604b54159f5e5ec83fe86555d..d66cc008f4b79ff2ebc53d424ec931959cc569d3 100644
--- a/src/blender_mcp/consent_prompt.py
+++ b/src/blender_mcp/consent_prompt.py
@@ -158,6 +158,12 @@ async def maybe_prompt_for_consent(ctx: Any) -> str:
     Safe to call on every tool invocation: returns "" on a lock check unless
     this is the first call of a conversation that needs to ask.
     """
+    # Do not ask to re-enable collection while the server is opted out.
+    from .telemetry import get_telemetry
+
+    if not get_telemetry().config.enabled:
+        return ""
+
     session_key = _session_key(ctx)
     if session_key is None:
         # Cannot tell conversations apart, so asking could repeat every call.
diff --git a/src/blender_mcp/telemetry.py b/src/blender_mcp/telemetry.py
index e5fda8c31195d13de10ad982f2cdb7f960d71a2c..b0b04f56975103085baa67ca61a26a972aba5d8b 100644
--- a/src/blender_mcp/telemetry.py
+++ b/src/blender_mcp/telemetry.py
@@ -65,19 +65,43 @@ class TelemetryEvent:
     metadata: dict[str, Any] | None = None
 
 
+@dataclass(frozen=True)
+class DisabledTelemetryConfig:
+    """Local opt-out state; deliberately has no endpoints or credentials."""
+
+    enabled: bool = False
+
+
 class TelemetryCollector:
     """Main telemetry collection class"""
 
     def __init__(self):
         """Initialize telemetry collector"""
-        # Import config here to avoid circular imports
-        from .config import telemetry_config
-        self.config = telemetry_config
+        # Consent describes the add-on, independently of server collection.
+        self._consent_cache: bool | None = None
+        self._consent_cached_at: float = 0.0
+        self._consent_lock = threading.Lock()
+        self._customer_uuid: str = ""
+        self._session_id: str = ""
 
-        # Check if disabled via environment variables
+        # Opt-out must not load optional config or create identifiers/workers.
         if self._is_disabled():
-            self.config.enabled = False
+            self.config = DisabledTelemetryConfig()
             logger.warning("Telemetry disabled via environment variable")
+            return
+
+        try:
+            from .config import telemetry_config
+        except ModuleNotFoundError as exc:
+            if exc.name != f"{__package__}.config":
+                raise
+            self.config = DisabledTelemetryConfig()
+            logger.warning("Telemetry disabled: optional blender_mcp.config is absent")
+            return
+        self.config = telemetry_config
+        if not self.config.enabled:
+            logger.warning("Telemetry disabled via configuration")
+            return
 
         # Generate or load customer UUID
         self._customer_uuid: str = self._get_or_create_uuid()
@@ -87,10 +111,6 @@ class TelemetryCollector:
         self._event_timestamps: list[float] = []
         self._rate_limit_lock = threading.Lock()
 
-        self._consent_cache: bool | None = None
-        self._consent_cached_at: float = 0.0
-        self._consent_lock = threading.Lock()
-
         # Background queue and worker
         self._queue: "queue.Queue[TelemetryEvent]" = queue.Queue(maxsize=1000)
         self._worker: threading.Thread = threading.Thread(
@@ -271,6 +291,8 @@ class TelemetryCollector:
 
     def _send_event(self, event: TelemetryEvent):
         """Send event to Supabase via the PostgREST API"""
+        if not self.config.enabled:
+            return
         try:
             data = {
                 "customer_uuid": event.customer_uuid,
diff --git a/src/blender_mcp/trajectory.py b/src/blender_mcp/trajectory.py
index 7a9421744eff32fa7a4f3bda205b05939faa1845..9c6489995d60c20e7241637a4d33a84fccdb59a9 100644
--- a/src/blender_mcp/trajectory.py
+++ b/src/blender_mcp/trajectory.py
@@ -1439,13 +1439,57 @@ class TrajectoryRecorder:
         return True
 
 
+class DisabledTrajectoryRecorder:
+    """Opt-out consumer interface: no workers, identifiers, capture, storage or network."""
+
+    def _can_write(self) -> bool:
+        return False
+
+    def note_client(self, name: str | None, version: str | None=None) -> None:
+        return None
+
+    def note_goal(self, goal_text: str | None) -> None:
+        return None
+
+    def note_agent_observation(self, *, modality: str, tool_name: str, summary: Any=None, screenshot_ref: str | None=None) -> None:
+        return None
+
+    def snapshot_world_state(self) -> dict[str, Any] | None:
+        return None
+
+    def maybe_auto_capture(self, state_delta: dict[str, Any] | None) -> str | None:
+        return None
+
+    def drain_human_activity(self) -> int:
+        return 0
+
+    def record_step(self, *, tool_name: str, goal_text: str | None=None, params: dict[str, Any] | None=None, raw_code: str | None=None, state_before: dict[str, Any] | None=None, state_after: dict[str, Any] | None=None, success: bool=True, error: str | None=None, duration_ms: float | None=None, screenshot_ref: str | None=None, blender_version: str | None=None, observation_kind: str='action') -> bool:
+        return False
+
+    def record_observe_step(self, *, tool_name: str, goal_text: str | None=None, modality: str, summary: Any=None, screenshot_ref: str | None=None, success: bool=True, error: str | None=None, duration_ms: float | None=None) -> bool:
+        return False
+
+    def record_feedback(self, *, feedback: str, correction_text: str | None=None, step_index: int | None=None, goal_text: str | None=None, source: str='agent_report', event_timestamp: float | None=None) -> bool:
+        return False
+
+    def close_episode(self, reason: str) -> bool:
+        return False
+
+    def flush(self, timeout: float=5.0) -> bool:
+        return True
+
+
+_disabled_recorder = DisabledTrajectoryRecorder()
+
 _trajectory_recorder: TrajectoryRecorder | None = None
 _recorder_lock = threading.Lock()
 
 
-def get_trajectory_recorder() -> TrajectoryRecorder:
+def get_trajectory_recorder() -> TrajectoryRecorder | DisabledTrajectoryRecorder:
     """Get the process-global trajectory recorder."""
     global _trajectory_recorder
+    if not get_telemetry().config.enabled:
+        return _disabled_recorder
     if _trajectory_recorder is None:
         with _recorder_lock:
             if _trajectory_recorder is None:
